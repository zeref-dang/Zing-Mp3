import React from "react";
import PropTypes from "prop-types";

const NewReleaseHome = () => {
  return <div>NewReleaseHome</div>;
};

NewReleaseHome.propTypes = {
  //
};

export default NewReleaseHome;
